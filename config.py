# Where the hardcoded configs live. Made in .py to save five seconds.
module_type_folder = "/home/lavaill/Workspace/Airships/data/ModuleType"

# The rock has a case value which wrecks the parser. It's the only module that has that property for now.
# It is ignored anyways but really, it's good to keep that peculiarity in mind.
ignored_modules = ["DROPPY_ROCK"]
